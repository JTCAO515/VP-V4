# VPJ-72 remaining out-of-scope acceptance

The complete owned-fixture offline workflow ran. These remaining items do not masquerade as
completed runtime capability:

- Actual human evaluation/calibration, reviewer identity verification, calibrated model judge,
  real provider A/B calls, adoption thresholds and production selection: UNRUN.
- #267/#268 real-language/provider/Trip/worker/budget/receipt/12-case acceptance remains with
  those Issues. Their current operational evidence is not recalculated or invalidated here.
- Holdout task content was not read, tuned against, evaluated or claimed blind. Only canonical
  case ID/group metadata is projected. The 20 authored variants are exposed development data.
- Real user text, C2 retention/deletion/human access, Auth, database, Staging and native/browser
  product integration were not exercised by this offline-only slice.
- External datasets/tutorial content, NC materials, model weights, public Hub/Space, cloud Jobs,
  training, purchases and provider calls were not adopted or executed.
- Human-source parsing is tested with an explicitly simulated in-memory record; no simulated
  human record is written into the delivered evidence. Fixture annotations remain fixture-only.
- Independent exact-HEAD review and required CI are pending with root.

The new report's `realProviderPairing`, `humanCalibration` and `adoption` remain UNRUN; cost and
usage remain unknown. These report values describe this offline scope, not a claim that existing
budget infrastructure or separately authorized runtime work is absent.

Rollback removes only the new derivative workflow/version adapter. Preserve existing pairing,
review state, feedback and evidence; do not change live routes, permissions, data or configuration.
