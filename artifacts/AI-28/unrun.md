# AI-28 unrun checks

- No Preview or Production deployment was attempted.
- No local Supabase/database/RLS runtime was available, so owner, other-user, immutable Proposal, expired Fact, and Canvas recheck interaction could not be exercised against a real session.
- No reviewed/current/licence-allowed public Explore projection exists. This Issue therefore adds no public POI control, source lookup, Proposal creation endpoint, or Trip mutation.
- No map, external data fetch, provider, credential, account, or release action was used.

The contract remains intentionally navigation-only. A future eligible projection must present an existing canonical UUID and call the separately protected Proposal workflow; it must not infer identity from card copy.
