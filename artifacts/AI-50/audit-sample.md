# AI-50 C0 audit sample

The deterministic security fixture records `{ reportId, reviewerId, disposition }` only after a
reviewer distinct from the author supplies the closed `verified` state. It contains no raw report,
identity document, legal decision, or external deletion receipt.
