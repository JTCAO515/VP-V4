# V4-01 unrun checks

- L2 runtime contract/integration, L4 security/eval, L5 browser/device, L6 staging and L7 production
  observation are not run because V4-01 is a governance registry only and changes no runtime, UI,
  database, provider, deployment or public capability.
- No framework package, Demo fixture, user, Trip, POI, price, weather value or provider result was
  imported. Framework candidates remain adopted baseline, spike, defer, conditional or reject exactly
  as stated in the adoption matrix.
- `artifacts/V4-01/commands.jsonl` is local and git-ignored. Required command metadata is recorded there;
  command output remains in local logs and GitHub CI.
