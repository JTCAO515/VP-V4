# Place grounding integration — in progress

Base: main `6e3a1289f4db048f0d3f87dfd9cb57795ec55782`, isolated branch `codex/place-address-grounding-20260914`. This continues #206/#264 after PR373; Maps draftPR372 is the sole independent preparation lane.

Implemented locally: `knowledge-statement/2` carries a reviewed place name pair and typed address or explicitly dated opening window, reusing the existing candidate, independent review and publication path. Values reuse GroundedAddressClaim/GroundedTimeWindowClaim shapes without pretending pending content is a GroundedClaim or EvidenceReceipt. v1 remains unchanged. Place identity is exact-name and city scoped; different subjects sharing a name yield ambiguity. New SQL helpers remain private. Migration application writes no content or activation state.

Validation: TypeScript typecheck, source lint267 and Ops contracts3/3 PASS. Isolated PostgreSQL full grounded suite18/18 PASS, zero skips (`db-first.tap`), including rollback of the new migration, pending/self-publication rejection, actual bilingual/case/spacing matching, cross-city and fuzzy-match denial, ambiguity, normal revocation, private-function denial for anon/authenticated/service_role and preservation of full v2 payload. Existing rail/payment/SIM, ownership, cancellation and historical-expiry checks remain passing. Independent review0Critical/0Important covers only this initial scope (`initial-review.json`).

The second local implementation now adds parameterized place questions, v6 literal-name routing, lease/dispatch/consent-bound completion, persisted selected subject, typed-value conflicts and Shanghai-date eligibility. Native and Web validate subject/name/value/scope and cap today's hours at local midnight; copy distinguishes missing address from missing hours. Historical answers retain their original subject and evidence gaps.

Second-stage local evidence: full contracts291/291 PASS (77 files, zero skips), isolated PostgreSQL19/19 PASS (zero skips), Web read9/9 PASS, native knowledge14/14 PASS and native build-for-testing PASS. Logs: `contracts-first.log`, `db-name-barrier.tap`, `web-read-first.tap`, `native-tests-first.log`, `native-build-first.log`. Earlier `db-bridge-first.tap` is retained as pre-fix evidence, not the final concurrency proof. Typecheck and source lint267 PASS. Web build PASS (`build-first.log`, then `build-typed-display.log` after the display fix). Final consumer review0Critical/0Important (`consumer-review.json`): an initial Important typed-value display omission was fixed in both actual views, and fixtures now use explanatory-only text. Web typed-display read9/9 PASS (`web-read-typed-display.tap`); native typed-display14/14 PASS (`native-tests-typed-display.log`), static tests22/22 PASS (`static-first.log`).

Backend review found one Important concurrency gap: a different subject with the same name could publish while completion waited on the selected publication. The fix orders a shared name-selection barrier before completion's row locks, and an exclusive barrier before normal publish/revoke row locks. The final database test demonstrates the blocked publication and later ambiguity without rematching saved history. Final backend review reports0Critical/0Important; consumer review is separate.

Remaining integration before delivery: required CI, encrypted fresh Staging backup and isolated restore/rollback, migration47/48 verification, bounded v6 real-model regression and native/Web producer/read observations. No place model calls, Staging migration, PR, merge or user acceptance yet.

Chosen direction: model returns a routing suggestion and exact place-name excerpt, not a subject ID, facts or aliases. Server resolves the excerpt against eligible reviewed names in the requested city. Unknown identity cannot borrow another place; ambiguity must request clarification. Existing place selection is fixed for historical reads. Address, today's explicitly dated hours and their combination retain independent required claims; hours from another local date or expired publication must not answer today's request. Preserve original gaps and do not expand saved answers. No new search engine, coordinator, table grants, Trip writes, recipient or Production action. Existing pending address assertions are not silently promoted; publication still requires explicit authored v2 input and independent review.

All held-out Harness definitions were already visible to this executor; no blind evaluation claim. Synthetic development cases must not become hardcoded name/answer logic.

Committed console logs normalize trailing whitespace only; original bytes remain in the protected local place-grounding cache under `raw-local-logs`. Initial raw-log diff check found console whitespace; it was corrected without changing test outcomes.
