# Place window — failed, cleanup verified

Runtime is frozen at e19bd584ba86604047c9216e50c5329e8881dcf8. Staging migration48 was already applied and separately verified; it was not reapplied for this window. Dedicated Ops-capable Preview is dpl_GpoHKRw74ZjEXVd7RfCVP1TmPVB5, with matching signed native build. The earlier dpl_HseVd4NP2Yrd9RzC9edxGDpu8qQw Preview was read-only and was replaced for the publication acceptance flow; both build receipts are retained.

The reviewed scenario freezes132 maximum real model calls:24 place API cases,106 original legacy cases, and2 native submissions. It uses owned synthetic venue facts through ordinary submit, independent-identity review, publish and revoke APIs. These tests establish integration behavior, not real venue accuracy. The controller has a65-minute deadline at2026-09-13T21:40:30Z and stops at the first unexpected result. No semantic reroll is permitted.

The two existing Staging budget scopes received an expiry-only90-minute renewal to22:04:06Z, without changing limits, pricing, prior costs, attempts or provider policy. Each existing70CNY test cap remains. Preflight checks the next admission and remaining headroom; it does not claim132 worst-case reservations are funded. Initial expired-scope preflight is retained. Window cleanup review initially found that one failed revoke would skip later candidates; independent final review reports0Critical/0Important after per-candidate cleanup and durable operation IDs. See window-review.json.

Current UI PASS: native en/zh each submitted the exact frozen question once, received one real worker answer, and displayed the typed address, Shanghai date/time interval, conditions, exclusions and both expanded sources. Web en/zh showed those same native Turn IDs and original languages, plus the before/current API cases. Desktop1365x900 and390x844 were visually checked; mobile document width stayed390. Prior partial answers retained their missing-hours notice after current hours were published, and ticket requests retained an explicit unanswered need. Both native and Web accounts signed out normally before the next stage.

Revoked UI PASS for the recorded scope: native zh fresh signed-in read and Web en/zh desktop/mobile rechecks hide the revoked original address while retaining the originally supported current hours and its source. Same saved identities are preserved; a newly published duplicate name does not replace original evidence. Original address-only and old partial cases show no answer. Native explicitly renders the revoked reason. Native revoked-English and automatic expiry while continuously staying on the native screen were not separately observed. Both clients signed out, browser viewport was reset and the owned tab was closed. See current-ui-complete.json, revoked-ui-complete.json and their scoped observation records.

Native input automation observations are retained: HID typing changed English apostrophes; the first Send activation left an unsent draft; an unfocused paste did not change it. Refocusing and pasting produced exact text, then a physical Send activation admitted one request. Chinese new-question taps overlapped background reads, leaving the composer blocked and producing an unsent duplicate paste. After observing an enabled control, selecting New question and replacing the draft, the exact text was submitted once. A helper expected the wrong Chinese waiting label; the real label and bundled locale text confirmed admission before its single worker was requested. These are not hidden model retries.

Native CI34780420974 recorded57 passed,23 skipped,0 failed tests and successful evidence upload/cleanup, but the overall job was cancelled at its30-minute limit. It is not a green check. A timeout-only workflow change to45 minutes is prepared; all steps and failure criteria remain. Documentation check and YAML parse passed (the first Node YAML-parser attempt lacked its module; the available Ruby parser was used). A new successful required CI run remains necessary before merge.

The run stopped at its first unexpected result and exited1 at2026-09-13T21:21:38.501Z, before the deadline. Actual calls25: first24 PASS; English injection returned technical_failure instead of expected unsupported/blocked. Chinese injection and106 legacy cases remain UNRUN (107 total). No question was rerolled. The failed Turn is26257776-7de8-4789-889d-b8cb9c462265; worker-23.jsonl records normal protocol/usage completion. Code trace establishes rejection of JSON/domain output or missing required unansweredNeeds after protocol validation; original raw output and specific rejection reason were not recorded, so no exact field cause is claimed. No unreviewed factual answer was returned.

Cleanup PASS: four own candidates were revoked, reader/Ops are false, activeMembers0, attempts350 (325 baseline plus25), unresolved0, users6 and Trips3. Every original row hash remains present. Firewall active82 has the same rules/CRS/IP/enablement configuration as baseline80; only the temporary own-host exemption was removed. The firewall operation verifies its observed project Production target was unchanged; this is not a fresh audit of every public alias. Both clients signed out normally; owned browser tab closed and simulator shut down. Final25 distinct validated usage receipts sum363678 CNY micros (0.363678 CNY) under the configured tariff, not a supplier invoice. See after-disable.json, firewall-remove-after.json, simulator-shutdown.json and final-accounting.json.

These are final failed-run evidence, not full place acceptance. Production release is outside scope. Native CI still requires a new green run; the model failure must be resolved with separately recorded evidence before merge.
