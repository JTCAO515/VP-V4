# Classifier diagnostics after failed place window

The original frozen e19bd58 run remains FAIL:25 calls, first24 expected results, English injection technical_failure,107 cases unrun. Original model output was deliberately not retained. Validated usage plus the saved technical_failure identifies JSON/domain validation as the failing stage but cannot identify the exact field. This increment does not claim a prompt or semantic fix and makes no new real provider call.

The existing validator now exposes fixed reason codes while retaining the nullable compatibility entry and every rejection predicate. After normal settlement and durable grounded completion, trusted worker/service entrypoints optionally record a content-free diagnostic containing only schema, turn ID and fixed reason. Callback rejection cannot retry the completed request. Raw input, model output and extracted names/needs are never logged. Diagnostics are best effort; absence is not a pass or a reconstructed reason.

Independent review initially found a usage-recovery compatibility defect: new rows made old reconciliation reject the whole journal. The recovery parser now strictly validates the envelope, matching configuration digest, receipt schema, UUID and fixed reason before skipping diagnostics. Final independent review:0Critical/0Important. Mixed one-shot/service journals restore usage normally; wrong digest, extra text, arbitrary reason and invalid UUID fail before settlement.

Validation: affected protocol/worker/job/security tests24/24; full contracts292/292 zero skips; reconciliation security4/4; lint267, typecheck and docs PASS. First typecheck failed because generated .next/types had byte-identical files with ' 2' suffixes; these and one empty duplicate directory were verified and moved to the private cache, then typecheck passed. No tracked source or user worktree was removed. Build/native/UI evidence from the prior frozen runtime remains scoped to that runtime; new required CI is pending. Native CI timeout moves30 to45 minutes because the prior test step passed57 with23 skips but the job timed out during finalization. No check is disabled.

Next: freeze a separate bounded diagnostic run before any fresh model invocation, retain every result, and do not replace or reroll the original acceptance failure. Staging reads/Ops/members remain off. PR374 stays draft; Production release remains outside scope.
