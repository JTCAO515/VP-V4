# VPJ-60 — synthetic OCR/vision slice, 2026-09-22

Related to [#200](https://github.com/JTCAO515/VP-V4/issues/200), not closure.
Baseline `ce46abd` (fresh origin/main); isolated `codex/vpj-60-media-qualification`.
Live GitHub #190 and #193 were both CLOSED on this run. Neither qualifies media.

## Implemented

- Operator-only C0 PNG transcription using existing Qwen HTTP transport, token usage type,
  attempt admission and durable-budget orchestration. No changes to shared gateway/profiles.
- Bounded image and response input, pinned model/output checks, error/partial/refusal handling,
  abort/deadline behavior, non-content lifecycle receipts and usage preservation on failures.
- Fixed bilingual synthetic cards and strict name/date/number/negation checks. Both rendered
  images inspected locally: readable, complete lines, no clipping (Arial Unicode, 960×560).
- Explicit no-network plan and <=3-call journaled CNY20 execution proposal. Old text campaign
  allowance is not reused. No actual credential content is printed or copied.

## Validation

- PASS: 13 focused/adjacent media tests (10 new, 3 existing), including real HTTP transport composition with a controlled
  fetch seam, malformed/oversized input, budget denial, cancellation, timeout, stream limits,
  usage validation, partial/safety/invalid output and bilingual semantic mutations.
- PASS: initial adapter typecheck; runner typecheck initially found a duplicate `kind` metadata
  key, corrected to a nested `outcome` without changing provider behavior.
- PASS: lint, typecheck, docs check and diff whitespace check. Campaign subprocess tests
  verify usage-before-settlement, cancelled/failed full holds, no automatic retries and
  duplicate-directory rejection. All provider traffic in local tests is controlled fetch.
- Full build/suites: delegated to unchanged Quality PR CI; results recorded on the PR.
- New Qwen live provider call: UNRUN — media-specific spending authorization requested;
  no request made. The earlier DeepSeek HTTP400 evidence remains in `verification.md`.

## Task-specific capability decision

| Task | Implementation / evidence | Qualification |
| --- | --- | --- |
| OCR/vision | C0 adapter, two rendered cards and local behavior checks | UNRUN real provider; disabled for users |
| ASR | No live provider adapter or audio corpus in this slice | UNRUN; disabled |
| TTS | No live synthesis/listening or read-aloud comparison in this slice | UNRUN; disabled |
| Screenshot import | Existing private-media helpers fail closed; no authenticated parser/policy exit | UNRUN; belongs to #201 integration |
| Chinese/English key semantics | Frozen exact-field scorer; mutation tests pass locally | Real quality UNRUN |
| Usage/cancel | Controlled transport receipts and abort tests pass | Actual usage/charge/upstream stop UNRUN |
| Region/retention/deletion | Beijing endpoint proposed; no provider file creation | Internal region/retention unknown; deletion UNRUN |

The runtime module and runner do not enable any client capability, write a Trip, upload user
materials, modify a database, publish content or deploy. This PR leaves #200 open.

See [scope, reproduction, sources and rollback](../../docs/benchmarks/media/vision-c0-v1.md).
