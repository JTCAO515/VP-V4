# Retained native interaction observations

The first fresh Simulator AX describe failed before any login, request or provider call; a subsequent describe confirmed signed-out Ask and login retry succeeded.

The original cancel helper scrolled from y1623 to1124, encountered an evidence-rechecking AX tree with actions hidden, then saw the target at y127 and clicked. Read-only server observation still returned accepted, so that click is NOT counted as cancellation. A later AX tree was back at the original scroll position. No conclusive root cause is claimed.

The following label-based disclosure tap exceeded45seconds. A coordinate tap on the currently inspected disclosure chevron collapsed the already-read notice; this is only display state, not changed consent. Subsequent cancellation uses the same original Turn. Each intent and readback is retained separately; no second question/provider call is submitted.
