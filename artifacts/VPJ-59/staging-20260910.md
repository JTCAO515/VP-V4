# VPJ-59 authorized Staging budget validation — 2026-09-10

User explicitly authorized20260909184816_vpj_59_durable_model_budget.sql on the same
selected VP-V4 Staging, refreshed backup/restore, two new ordinary Auth fixtures and
synthetic budget records with exact-ID cleanup. No provider call/recharge/Production DB.

- CLI target name/SG region/healthy status rechecked. Initial3 Auth/2 Trip/25 migrations.
- Fresh affected-schema archive encrypted directly with AES-256-GCM; key in macOS
  Keychain, no plaintext archive or credentials in repo/logs. Source digests stable.
- Fresh no-network/no-published-port PostgreSQL restore PASS:3 Auth/2 Trip/25 history and
  exact Auth/Trip digests; temporary restore container removed.
- All26 local migration files byte-identical to reviewed merge8a465f9. Native CLI dry-run
  through Session pooler verify-full/official CA listed exactly the new budget file.
- Append-only push used --skip-vault, no seeds/custom roles. Exit0; actual remote history26
  matches each local version/name. Original Auth/Trip full row digests unchanged.
- Independently reviewed execution and probe scripts:Critical0/Important0 before execution.
- Actual ordinary password login/Auth-user checks and HTTP budget matrix40/40 PASS.
  Owner/other/anon ledger read/configuration and all3 budget RPCs are denied; service
  wrong-owner input rejected; concurrent same-ID reserve/dispatch grants once; pending
  holds full amount/concurrency, cannot refund; settlement is idempotent and rejects
  conflicting costs; observed synthetic overrun freezes subsequent admission.
- Only pre-recorded fixture scope/attempt IDs and provider composite key were removed,
  then the two newly created Auth IDs. Cleanup PASS; original3 Auth/2 Trip digests match.
  No audit records were deleted. All budget values were synthetic; providerCalls0/cost0.

This proves the named Staging schema and scoped budget HTTP behaviors. It does not prove
an actual paid provider attempt, a deployed worker host, authoritative native sessions,
real-user policy or complete Harness acceptance. #194/#189 remain OPEN. Direct-host
connection failure is not changed by Session pooler or HTTP success.

Exact backup/fixture metadata remains private outside the repository. Applied SQL remains
append-only; recovery uses reviewed forward changes rather than rewriting migration history.
