# Translation navigation regression follow-up — 2026-09-22

- Remote head investigated: `d718b1b8be170e82373eb80888e7d7d698e3e5b9` (latest main merged into this PR). Local checkout was clean and fast-forwarded before editing.
- [Native CI run 35673411349](https://github.com/JTCAO515/VP-V4/actions/runs/35673411349) passed toolchain preflight and reached tests. The downloaded native evidence `tests.log` identifies `AppShellUITests.swift:102`: `XCTAssertTrue` for the obsolete text `Planned bilingual text, speech, and image assistance.` failed. UI suite: 31 tests, 21 explicitly skipped, 1 failure. Skips are not acceptance.
- Classification: this PR missed updating a test of the deliberately replaced translation placeholder. No evidence of a translation-model failure; no real provider was invoked.
- Repair: replace the obsolete placeholder assertions with the actual text form, sign-in notice, ability to enter a synthetic phrase and disabled submit without authentication/consent. Preserve default Ask, five tabs, navigation into Translation, and the separate Today/Trip navigation stack. No production code, CI filters, runner or simulator configuration changes.
- #478 owner confirmed this method has no concurrent changes; their separate full accessibility tests are retained unchanged.
- First local rerun: the new form/input/disabled-submit assertions passed, but the added typing left the keyboard over the Tab bar; the subsequent Trip hit point was `{-1, -1}` and Today assertion failed. The test now exercises unchanged tab isolation before typing, then returns to Translation and verifies the draft/disabled submission. No assertion was removed to conceal that failed attempt.
- PASS: final focused `AppShellUITests/testEnglishTabsTodayAndTranslationRoute`, 1 test / 0 failures, dedicated iPhone 17 Pro iOS 26.5 simulator `FC6D13AB-EE1C-4599-A25B-C31C37D508DB`, Xcode 26.6. Result: `/tmp/vpj26-derived/Logs/Test/Test-VisePanda-2026.09.22_9-45-02-+0800.xcresult`. Signed-out navigation/input is now observed; model/card semantics remain outside this test. `pnpm docs:check` and diff review passed. The prior deterministic/Preview checks passed for d718b1b; new head CI must run after pushing this test change.
- Still UNRUN: real-provider semantics, deployed authorized translation/worker/budget path, large-card on-device/VoiceOver/maximum-text acceptance. A signed-out navigation test does not replace these.

Remote `e9d85fe8` independently fixed the same retired placeholder assertions while the focused test was running (author JTCAO515; commit credits Claude Sonnet 5). The unpublished local commit was rebased onto it without force-pushing. Its real heading, signed-out notice and disabled-submit assertions remain; the combined test retains the local input/navigation checks. Executable lines were compared against the locally passing d07dbeaf test and are identical; only the remote explanatory comment was added, so the same local test evidence is reused.
