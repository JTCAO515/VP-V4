# VPJ-61 outstanding acceptance

UNRUN: remote schema/role rollout (not authorized), real user archive, target
native/API/GoTrue/Supabase readback, physical-device interaction/VoiceOver,
Pass-expiry result/export readback, #199 preference-choice consent integration and
#224 human service lifecycle. No provider or paid calls, production actions,
account deletion, existing simulator/DB reset or global handoff write.

NOT IMPLEMENTED in this first archive slice: 3-draft/1-Active capacity and explicit
Active transition/legacy reconciliation; per-preference selection UI. Current
confirmed snapshots are not silently reclassified as Active. #240 remains OPEN.

Local SQL uses the real application migration chain and SQL claims fixture in a
network-isolated disposable database; this proves database behavior, not real JWT,
provider, human-service fulfillment or user acceptance. Native state tests use a
synthetic URLProtocol and cannot substitute for target-environment testing.

Current ServiceTasks require an unbound thread. Trip-associated human-service
preservation and status display must be verified with #224's actual producer.
