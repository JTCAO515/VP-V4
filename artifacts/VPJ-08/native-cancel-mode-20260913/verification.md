# Grounded-only native cancellation — local verification

Fixes the observed valid-owner503 on API2717bf9/Staging42. The shared cancel endpoint now uses the accepted native runtime/current session and unchanged cancel_chat_turn owner/terminal rules, without selecting a legacy generation mode/policy. Native UI/protocol and ledger semantics are unchanged. Local cancellation requires the existing native Trip runtime flag; deployed access remains scoped to configured Staging Preview.

PASS: actual disposable GoTrue/SQL/HTTP grounded suite1/1 with legacy generation disabled. Covers native admission, real HTTP event disconnect/replay, six synthetic model requests, foreign-owner cancellation403, missing credential401, cookie/origin/query/nonempty body rejection, owner cancellation with source reader disabled, exact repeat, one cancelled terminal/attempt, completed-answer rejection and logged-out native session401. Existing legacy-text HTTP suite1/1 also passed. No real supplier call in these local tests. Request/deadline/security19/19, lint/typecheck and full Next build passed. Independent permission review0Critical/0Important; runtime hash recorded.

Retained failures: first test setup omitted the existing Web read flag and stopped before the cancellation regression; fixture now explicitly enables its tested Web surface. Second run reproduces the intended legacy-disabled503 defect. First fixed run passed the cancellation branches then failed its logout expectation because prior browser global signout had already revoked the native session. The test now verifies native logout before browser global signout; final grounded suite passes. No assertion weakened and runtime unchanged for that ordering correction.

Subsequent exact fixed Preview/native cancellation and empty-worker/no-extra-call readback passed; see ../native-cancel-staging-20260913/verification.md. That original task is now cancelled with its one settled4308micros attempt unchanged. Final evidence CI/merge remains pending. PR348 usage recovery is separately merged9e24faa with actual Production CANCELED and aliases preserved. No production/full#195/#196/S2 claim.
