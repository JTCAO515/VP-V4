import assert from "node:assert/strict"; import test from "node:test";
import {IdempotencyConflictError,TurnContractError,TurnEventLog,TurnRequestRegistry} from "../../../lib/server/turn/contract.ts";
test("replays ordered buffered events and permits one terminal",()=>{const log=new TurnEventLog();log.append({turnId:"t",eventId:"1",type:"accepted",state:"accepted"});log.append({turnId:"t",eventId:"2",type:"phase",state:"planning"});log.append({turnId:"t",eventId:"3",type:"terminal",state:"completed"});assert.deepEqual(log.replay(1).map(x=>x.sequence),[2,3]);assert.throws(()=>log.append({turnId:"t",eventId:"4",type:"phase",state:"generating"}),TurnContractError)});
test("reuses the same key and rejects a changed digest",()=>{const registry=new TurnRequestRegistry();assert.deepEqual(registry.register("key","a","turn-1"),{turnId:"turn-1",reused:false});assert.deepEqual(registry.register("key","a","turn-2"),{turnId:"turn-1",reused:true});assert.throws(()=>registry.register("key","b","turn-3"),IdempotencyConflictError)});
