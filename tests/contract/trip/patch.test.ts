import assert from "node:assert/strict";import test from "node:test";import {applyPatch,assertTripPatch,InvalidTripPatchError} from "../../../lib/server/trip/patch/contract.ts";
test("requires versioned closed patch operations",()=>{assert.doesNotThrow(()=>assertTripPatch({expectedVersion:0,operations:[{kind:"set_title",title:"Beijing"}]}));assert.throws(()=>assertTripPatch({expectedVersion:-1,operations:[{kind:"set_title",title:"x"}]}),InvalidTripPatchError);assert.throws(()=>assertTripPatch({expectedVersion:1,operations:[]}),InvalidTripPatchError)});
test("applies closed patches deterministically",()=>{const patch={expectedVersion:1,operations:[{kind:"upsert_day" as const,dayId:"d1",date:"2026-10-01"}]};assert.deepEqual(applyPatch({version:1,title:"Trip",days:[]},patch),applyPatch({version:1,title:"Trip",days:[]},patch));assert.throws(()=>applyPatch({version:2,title:"Trip",days:[]},patch),InvalidTripPatchError)});
