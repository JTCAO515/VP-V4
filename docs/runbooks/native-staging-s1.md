# S1 native and same-Trip Staging acceptance

Related to #191/#192, baseline main7bf22fb plus the reviewed native Preview increment.
This is the concrete next integration operation, not a completed execution record.

## Deployment and configuration

1. Review the exact code and pass the affected auth/Trip/Swift checks. Retain the existing
   staging33 history and data; this increment adds no migration. Record the exact Git SHA.
2. Use an explicitly approved Preview branch of `jtcao515s-projects/vp-v4` only. Configure
   branch-scoped `VISEPANDA_NATIVE_STAGING=true`, `VISEPANDA_TRIP_PROTOCOL_V2=true`, and server-only
   `VISEPANDA_NATIVE_STAGING_PROOF_KEY` through the existing secure official channel. Obtain the
   key only in the controlled process; do not print it, pass it in argv, commit it or request it
   in chat. Its permitted use is the existing fresh-password proof RPC. Do not change public
   database/key bindings, Production variables or team membership. Record names/targets only.
3. Deploy the reviewed branch as Preview. Read the actual deployment ID, SHA, `VERCEL_ENV`,
   `VERCEL_URL`, branch-scoped configuration and named Staging binding. The generated deployment
   host must match the project's accepted hostname family. The existing maintenance WAF must
   allow that exact reviewed host before requests; follow its existing concurrent-drift checks
   and preserve all other rules. Never use a wildcard or remove the maintenance rule.
4. Build the app with `VP_NATIVE_ENVIRONMENT=staging` and
   `VP_NATIVE_STAGING_API_ORIGIN=https://<verified deployment host>`. Inspect the final merged Info.plist
   and record its public origin and exact source SHA. No server secret belongs in the app.
   Existing Simulator signing is sufficient for Simulator evidence; physical/TestFlight evidence
   remains distinct. Do not publish Production or upload a Store build under this operation.

The current Goal permits preparation and staging work within existing authorization. Earlier
maintenance explicitly excluded native activation. Before executing new privileged-key provisioning
or expanding remote activation, resolve the exact configuration/account scope against the current
operator authorization. Repository merge currently triggers Vercel Production and requires the
separate release boundary resolved. Neither issue prevents code/testing/PR preparation.

## Ordinary-account user result

Use only explicitly authorized synthetic test accounts, generated by the controlled test harness;
no access to the three original accounts or their existing Trips. Record newly created IDs privately
for exact cleanup, never in repository evidence. Do not disable RLS or use a service-role Trip client.

- Phone A signs in, reads its profile, refreshes and relaunches with Keychain persistence.
- Phone B signs in to the same account: A loses access, cannot regain the mobile epoch through
  refresh or direct PostgREST, while the independently established Web cookie session continues.
- A different account cannot read/write the first account's profile, Trip, proposal or snapshot.
- Native creates a Trip and Day/Item proposal; display the actual diff and explicitly confirm.
  Web reloads that same UUID/version and content. Web edits via its existing proposal/confirmation
  path; native reloads the identical confirmed snapshot. Stale digest/CAS/replay and refusal
  preserve confirmed content and unsent edits. No hidden or unconfirmed Trip mutation is allowed.
- Logout/replacement/expired token is denial; temporary network/503 keeps credentials/drafts hidden
  and retryable. Redirects never forward credentials. Record an interrupted write as unknown ack
  until its original operation/receipt is reconciled.
- Exercise the affected Web route at desktop and390×844, and the native screen in both languages.
  Record physical device/iOS17/VoiceOver conditions separately when unavailable.

Clean up only exact records created by this harness under the authorized test cleanup scope;
verify cleanup and preserve all original data, revoked state and applied migrations. Record version,
environment, actual result, failures, skips and cleanup. No model/knowledge/policy or worker is
activated. S2 real Ask remains incomplete after this S1 check.

## Stop and rollback

If the host/branch/configuration or WAF has drifted, keep activation closed and re-read that state.
Disable only the owned branch activation and rebuild that Preview; for immediate stoppage remove
only its exact host allowance using the current maintenance version. Preserve original rules and
other approved hosts. Remove the app's Staging build configuration. Do not revert the database,
revive revoked sessions, rotate public keys or alter Production to recover this test.

Build reference: [Apple information-property-list guidance](https://developer.apple.com/documentation/bundleresources/managing-your-app-s-information-property-list) explicitly excludes arbitrary user-defined `INFOPLIST_KEY_*` settings from generation. The committed input plist is merged with the standard generated bundle settings; inspect the built values instead of assuming a custom build setting became a plist key.
