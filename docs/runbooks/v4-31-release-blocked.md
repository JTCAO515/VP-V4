# V4-31 release blocked runbook

Do not release VisePanda as full Demo parity while `docs/acceptance/v4-31-parity-audit.md` has a
`blocked` verdict. The next technical action is to implement a missing durable contract, then rerun
the full audit; no operator approval can replace its missing evidence.
