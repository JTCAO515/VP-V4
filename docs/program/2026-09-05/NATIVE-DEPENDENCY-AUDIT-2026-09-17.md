# 原生依赖复核 — 2026-09-17

用户确认将可并行开发的关系改为普通集成/验收引用。此前 PR #442 清理的是 status:blocked 标签，保留了全部217条原生关系；因此“blocked清零”仅指标签，不能描述 GitHub 图标。

本次以 main@ffcd194 和逐票实时快照复核；提交前吸收并行主线合并，不覆盖其实现或交接。保持原验收、任务身份、勾选、证据和全部技术输入；blockedBy 用于原生硬依赖，acceptanceDependencies 保留最终集成输入。两者联合仍是原217条无环任务图。不开新票，不改业务代码，不声明真实能力通过。

## 决策与边界

- VPJ-02/49/62/75/76 的定义、正文及任何方向的原生关系均冻结；不动其开发分支。
- 已关闭任务保留历史关系。可独立推进的现有实施/测试切片不必等上游整票关闭。
- 真正缺真实步行输入、同一局部改稿故障链、发布后观察、航班来源和扩展证据的任务保留原生依赖。接口或证据补齐后可再次复核，不永久锁死。
- 每行给出独立交付和逐边处置；“转换”仅转换追踪方式。未验收输入仍阻止相关集成结论/整票关闭，fixtures、计划、代码存在不冒充通过。
- 依据：main 中 issue-plan 的 executionBrief/复用入口、各票现存证据、docs/harness/README.md 的消费者清单与真实运行门。保护关系不作就绪重判。

## 逐票记录

### VPJ-04 / #191

首个切片：复用已有原生登录、换机和Web并存链，针对自然过期或丢失回执完成一条尚未验收的真实恢复路径。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-01 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-02 | 保留：保护范围 | 涉及正在开发的保护任务；本轮不重判、不改原生关系。 |

### VPJ-05 / #192

首个切片：复用已落地的两端Trip/Proposal接口，补一个并发改动、未知确认回执或硬锁显示的实际缺口。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-04 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-06 / #193

首个切片：从已有HTTP适配器和真实C0记录开始，固定同批中英输入，验证一个尚缺的provider协议/usage路径，再形成同口径成本质量对照。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-03 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-07 / #195

首个切片：在现有持久Ask上补一个中英可用结果与下一步的实际缺口；必要澄清/修复沿同一ServiceTask，核对回执与成本。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-04 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-05 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-06 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-59 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-08 / #196

首个切片：复用现有cursor/SSE与取消接口，在同一已接受任务上验一次后台/断网/重启后的事件接续和最终结果读取。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-07 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-09 / #197

首个切片：用日期未定且已有偏好的输入产出可修改相对日草稿，展示局部变化并沿既有确认路径保存/重载。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-05 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-07 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-08 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-10 / #198

首个切片：在真实Trip选中一天或项目，提出局部改动并显示增删/移日、代价和保留项；确认后回到原位置且重载一致。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-09 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-11 / #199

首个切片：把一个明确保存且有同意范围的基础偏好接入真实任务消费者，验证纠正/本次不用/撤回及迟到响应。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-03 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-05 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-07 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-12 / #201

首个切片：单张合成旅行截图经现有材料边界解析，展示原文定位和与Trip的新增/重复/冲突，经用户校正形成提案。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-01 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-03 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-04 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-07 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-60 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-13 / #203

首个切片：先接一个无日期新想法入口取得可采用成果，补回访上下文；随后分别贯通材料和在途入口，按真实事件记录首值。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-09 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-11 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-12 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-62 | 保留：保护范围 | 涉及正在开发的保护任务；本轮不重判、不改原生关系。 |

### VPJ-14 / #204

首个切片：复用现有Ops身份和候选审核，补普通授权账号在真实页面提交/异人审查/撤权的尚缺交互证据。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-02 | 保留：保护范围 | 涉及正在开发的保护任务；本轮不重判、不改原生关系。 |
| VPJ-03 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-15 / #205

首个切片：从已有已审中英声明和发布记录中选一个缺覆盖场景，补真实来源→异人审核→发布/撤回→产品读回。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-14 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-16 / #206

首个切片：复用已完成的直接知识回答与PR #416的MIRACL/BIPIA证据，补一条其余尚未验收的真实claim覆盖、partial或过拒答路径。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-07 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-15 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-17 / #207

首个切片：消费现有来源版本/撤回信号，完成一个下游投影或TripItemSupport的影响记录、ack和失败重试。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-05 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-14 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-16 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-75 | 保留：保护范围 | 涉及正在开发的保护任务；本轮不重判、不改原生关系。 |

### VPJ-18 / #208

首个切片：复用#362已完成探针和高德主选决定，补一个确实未验的使用场景、字段用途或成本/配额记录；不重跑整批取数。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-03 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-19 / #209

首个切片：沿#363的现有身份/搜索/详情/geocode继续一个未完成适配与地图同选中路径，然后在#364验路线出口。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-05 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-18 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-20 / #210

首个切片：让一条当前有资格的Explore内容完成浏览→Save→Ask→经确认Add到现有Trip的用户行为。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-09 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-15 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-19 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-21 / #211

首个切片：用一项有证据的支付/网络/预约准备事项，对unknown、已满足、不满足及不适用用户状态给不同下一步。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-10 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-16 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-19 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-74 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-22 / #212

首个切片：先用已有官方酒店出口验证一组hotel/date/occupancy参数在真机落地；不可用字段向用户说明。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-03 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-23 / #213

首个切片：沿已有日期/人数和行程比较住宿区域与可核实候选，保留已订/暂缓意向，选择后接回Trip调整建议。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-11 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-19 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-22 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-24 / #214

首个切片：从一次酒店外跳返回导入允许的订单材料，校正日期/地址/条款，识别重复/冲突并保存外部引用。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-12 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-23 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-25 / #215

首个切片：从已确认Trip形成许可内的Today/地址缓存，验证断网读取、版本显示和回网重验。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-04 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-05 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-26 / #216

首个切片：一个真实文字短句或当前地址生成中英译文与大字卡，核对否定/金额/地点且保留原文。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-07 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-16 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-27 / #217

首个切片：用一段许可内短音频完成按需录音→ASR→翻译→字幕/TTS，并验证停止或来电中断。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-03 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-26 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-60 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-28 / #218

首个切片：选一个已覆盖地点，将有来源短讲解、字幕播放进度与一条追问接回当前Trip。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-15 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-19 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-27 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-29 / #220

首个切片：以疲劳或有效晚点/关闭为输入，在当前Trip生成1–2个可理解的局部恢复候选，确认后应用。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-10 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-21 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-25 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-65 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-30 / #221

首个切片：先做一个用户亲设时间提醒，发送前核对当前Trip版本、用途同意、时区、完成/撤回状态。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-11 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-17 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-25 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-31 / #223

首个切片：在现有CaseRequest/AccessGrant下，只投影一个服务任务必需的用户字段，并验证纠正/撤回后的访问。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-03 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-11 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-12 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-14 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-57 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-32 / #224

首个切片：在受控容量下完成一次请求→排队→接单/分派→外部等待→有证据结果的可见服务流程。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-21 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-31 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-57 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-33 / #225

首个切片：冻结一个版本化Free/30天Pass开发权益表及交易时间规则，给sandbox配置和用户展示提供同一输入。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-03 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-34 / #226

首个切片：按33的版本化商品策略完成一次StoreKit sandbox交易验证→账号grant→两端生效及同交易恢复。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-04 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-05 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-33 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-35 / #227

首个切片：复用34的grant和59的内部费用账本，实现一个ServiceTask容量预留/结算/失败恢复及并发最后一份额度。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-06 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-08 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-34 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-36 / #228

首个切片：先让Trip/chat之一完成身份复核→异步导出或删除→回执，再接入原验收指定的核心模块handler。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-03 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-04 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-11 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-12 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-31 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-34 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-37 / #229

首个切片：基于已有budget-scope只读模型贯通一次实际attempt对账与能力停用/恢复，明确unknown费用。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-07 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-35 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-38 / #230

首个切片：复用已有恢复工具，在隔离目标验证一个实际备份恢复并重放删除/撤权tombstone，记录测得RPO/RTO。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-02 | 保留：保护范围 | 涉及正在开发的保护任务；本轮不重判、不改原生关系。 |
| VPJ-36 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-39 / #232

首个切片：先选媒体、OTA外跳、StoreKit或通知中的一个已接通路径，在真实境内外网络记录失败/恢复与时延。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-07 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-19 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-27 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-63 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-23 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-34 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-30 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-55 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-40 / #233

首个切片：在一个已运行的原生用户路径上验证选区/焦点/大字/VoiceOver与同Trip版本，随后覆盖全部原定模块。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-13 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-20 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-21 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-23 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-25 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-27 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-28 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-29 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-30 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-34 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-48 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-64 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-49 | 保留：保护范围 | 涉及正在开发的保护任务；本轮不重判、不改原生关系。 |
| VPJ-55 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-61 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-36 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-41 / #234

首个切片：在真实Web账号上完成一次Trip读写/差异确认/重载，并验证桌面与390宽度、中英、键盘和失败结果。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-05 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-20 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-34 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-42 / #242

首个切片：选定同一可安装TestFlight版本，让受控用户完成一个Plan→Ready→Travel旅程并保留跨模块异常证据。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-08 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-16 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-21 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-24 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-28 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-29 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-32 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-35 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-36 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-37 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-38 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-39 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-40 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-41 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-48 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-55 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-56 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-58 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-61 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-64 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-65 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-49 | 保留：保护范围 | 涉及正在开发的保护任务；本轮不重判、不改原生关系。 |

### VPJ-43 / #243

首个切片：先形成可审查的独立Production配置/迁移/回退包；取得指定环境执行授权后部署并核对真实状态。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-38 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-42 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-44 / #244

首个切片：复用已验版本准备准确中英商店资料、reviewer路径、隐私/删除/IAP说明，按实际账号完成获准提交。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-34 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-40 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-43 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-45 / #245

首个切片：在已交付版本执行72小时系统及7天机会相关观察，归档未解缺陷、支持/事故责任和产品发布结论。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-42 | 保留：实际输入 | 客户观察必须基于同一实际交付的Beta验收版本。 |
| VPJ-44 | 保留：实际输入 | 发布后72小时/7天观察不能在真实发布前生成。 |

### VPJ-47 / #247

首个切片：对一个事前定义的有效旅客队列形成激活、机会回访、净付款/退款和人工成本观察，给出继续/调整/证据不足结论。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-42 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-44 | 保留：实际输入 | 真实自助付费/退款经营观察需要实际可购买的获准商店版本；sandbox不能替代。 |
| VPJ-46 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-48 / #235

首个切片：用一条旅行体验投稿走注册用户提交→异人审核→发布或拒绝→作者撤回的受控链。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-14 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-20 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-36 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-50 / #248

首个切片：保持expand门：先用真实召回失败归因决定是否启动；启动后同批baseline对比一个检索变量并记录净收益。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-16 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-17 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-76 | 保留：保护范围 | 涉及正在开发的保护任务；本轮不重判、不改原生关系。 |

### VPJ-51 / #249

首个切片：保持expand门：对实际存在的中国航线需求，按同一字段/时效样本比较最少候选来源与账户能力。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-03 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-39 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-52 / #250

首个切片：来源51合格后，让一条实际航班状态变化生成相关Trip重验候选，覆盖unknown/过期/撤权。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-17 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-29 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-51 | 保留：实际输入 | 授权航班来源/字段/时效尚需来源评估，不能伪造实际航班输入。 |

### VPJ-53 / #251

首个切片：保持expand门：以真实需求及成本证据形成Android或新增语言的先后决定和一个明确实施范围。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-45 | 保留：实际输入 | 扩展决策须已有首发交付/支持观察。 |
| VPJ-47 | 保留：实际输入 | Android/新语言扩展须真实需求与经营证据，不能先搭空白客户端。 |

### VPJ-54 / #252

首个切片：保持expand门：用重复旅行价值、单位经济性和责任证据决定是否探索长期订阅或某一类交易深度。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-45 | 保留：实际输入 | 长期订阅/交易深度须首发交付与责任证据。 |
| VPJ-47 | 保留：实际输入 | 订阅和交易升级须重复旅行价值与单位经济性证据。 |

### VPJ-55 / #236

首个切片：在已验单图材料链上增加一类限定PDF或一个Files/Share入口，校正、去重并接回登录前的原任务。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-04 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-12 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-56 / #237

首个切片：复用已通过的原生CI；先将PR #421已合入testing-chat-vpv4但尚未入main的签名手册独立核对接回，再完成一个实际签名Archive/TestFlight安装缺口。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-01 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-57 / #222

首个切片：让用户创建一项请求、预览最少字段并授予有期限访问，验证撤回/到期/换员工/越权拒绝。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-03 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-04 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-14 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-58 / #239

首个切片：复用36已交付handler注册，选一个后来接入模块做删除期间并发与恢复后隔离，随后补齐原全模块矩阵。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-30 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-32 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-34 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-36 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-48 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-64 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-24 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-49 | 保留：保护范围 | 涉及正在开发的保护任务；本轮不重判、不改原生关系。 |
| VPJ-55 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-28 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-61 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-25 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-60 / #200

首个切片：用许可内合成材料验证一个OCR/vision、ASR或TTS实际任务与关键语义，分别记录usage、取消和数据生命周期。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-03 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-06 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-61 / #240

首个切片：从一个已确认Trip执行用户主动归档，保留未完服务与可读成果，再验证下一Trip不继承旧时效约束。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-05 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-11 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-32 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-63 / #231

首个切片：复用已有登录/Ask/Trip入口，从一个真实网络来源开始记录DNS到模型的可达/延迟/失败，再补另一地域。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-04 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-07 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-64 / #238

首个切片：在已有投稿链上完成一次举报→运营处置→回执，再加入屏蔽/申诉/删除和引用失效验证。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-14 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-36 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-48 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-65 / #219

首个切片：把一个相对日草稿补成真实地点/路线/时区/预约与行李缓冲约束，输出有来源的可行/待核结论和提案。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-09 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-16 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-19 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-67 / #264

首个切片：先核对PR #420的H04离线切片（合入testing-chat-vpv4，尚未在main），独立接回可复用增量；再把一个仍NOT_RUN的只读场景接到真实producer/consumer并核对Trip不变。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-66 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-16 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-68 / #265

首个切片：在实际路线依据就绪时执行同一Trip少走路且保留晚餐的完整局部改稿，取得确认/重载与测量回执。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-67 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-10 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-11 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-65 | 保留：实际输入 | 真实少走路比较尚需同口径地点/路线依据；已有transfer minutes不是步行量。 |

### VPJ-69 / #266

首个切片：在68同一任务的一个提交/回执检查点注入真实中断，验证原任务恢复、取消和重复投递的终态。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-68 | 保留：实际输入 | 故障注入必须复用68的同一真实局部改稿任务及提交检查点，不能以fake替代。 |

### VPJ-70 / #267

首个切片：复用已交付72盲评包，在同一真实只读任务上冻结基线与一个候选、阈值/预算，形成可复核配对结论。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-67 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-72 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

### VPJ-71 / #268

首个切片：整合同一选定版本的两条核心任务、12场景与Ops停止/恢复，先证明失败可阻断，再给能力发布判定。

| 上游 | 处置 | 理由 |
| --- | --- | --- |
| VPJ-69 | 保留：实际输入 | 最终门必须有同版真实故障/取消/重连链，不能拼fixture结论。 |
| VPJ-70 | 保留：实际输入 | 最终门需要事前冻结且经配对/校准的选定配置。 |
| VPJ-37 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |
| VPJ-63 | 转为集成/验收引用 | 按本票首个切片使用现有合同/已合并消费者推进；上游整票完成属于最终集成验收，不是所有开发的开工条件。 |

## 地图子票

| 下游 → 上游 | 处置与理由 |
| --- | --- |
| #363 → #362 | 保留已完成探针的历史关系，不产生开放阻塞 |
| #364 → #363 | 普通关联；place identity/detail/geocode已在main，可先做路线适配，完整地图同选中仍待验 |
| #365 → #364 | 普通关联；先做地点Save/Ask/Add消费者，路程驱动改稿仍需真实路线输入 |
| #366 → #364 | 保留硬依赖；本切片直接消费当前真实路线及替代方案，不能编造交通变化 |
| #367 → #364 | 普通关联；先用#362已完成对照判定补查收益，无收益可明确保持关闭；集成时仍验完整路线 |

## 验证与回退

实际更新66张正文、转换192条原生关系；82张票读回通过。2026-09-17T05:44:59Z快照中开放票Blocked由68降至16：48张来自本轮分类调整，另4张来自并行PR #439关闭#241。本轮不修改#241或其双向关系。#190也被并行PR #443关闭，原生图标统计不受其影响。剩余图标随其他任务实际完成继续变化。同步日志为时间点证据，不是常量。对照快照逐票验证：无原验收删除、两类依赖并集不变、保护任务及关联边不变、正文已勾选与追加历史不变。GitHub 图标按剩余开放原生上游计算，不宣称全部清零。

回退使用本轮同步前快照重新添加移除的原生关系（issue database ID），正文按保留并发进度的反向补丁恢复；先检查最新状态，不能覆盖开发期间的新正文。仓库定义/生成器可 revert 本 PR，随后对照快照恢复图关系。无生产发布或业务数据变化。
