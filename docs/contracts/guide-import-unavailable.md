# Guide import unavailable v1

V4-28 owns the five-locale, RTL Guide import route. Owner-scoped artifact storage, purpose, TTL, extraction isolation and correction contracts are absent, so it does not accept a PDF, link or image and never retains or parses user content.

The route has no upload, artifact request, cache, extraction or state mutation. Roll back with a normal UI revert.
