# Tool surface unavailable v1

V4-22 owns the five-locale, RTL Tool route. Provider health observations, an owner-isolated offline cache, and an authorized Tool call are absent, so the route renders an unavailable boundary rather than a fictional healthy, degraded, or offline state.

It does not invoke the Tool Gateway, create Trip state, render a Tool Card or Proposal, or treat a feature flag as authorization. Roll back with a normal UI revert.
