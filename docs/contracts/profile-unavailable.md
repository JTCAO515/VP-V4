# Profile unavailable surface v1

V4-16 owns the five-locale/RTL Profile route while owner-scoped durable Profile storage is absent. It shows only unavailable: no account profile, preference, locale, currency, unit or default-time value is read, inferred or persisted.

The route makes no request or mutation. A later owner-scoped Profile contract must replace this surface before profile controls appear.
