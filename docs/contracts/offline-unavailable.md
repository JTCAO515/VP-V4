# Offline pack unavailable v1

V4-30 owns the five-locale, RTL Offline pack route. Encrypted owner-isolated cache, expiry record, logout cleanup and deletion cleanup are absent, so it stores and displays no Trip, address card, Safe Phrase or expiry state.

It never presents cache as live information and makes no cache, request or state mutation. Roll back with a normal UI revert.
