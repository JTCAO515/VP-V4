# VPJ-09 bounded relative-day outline

The native Trip screen accepts a bounded 2–7 day request naming Shanghai, Beijing, Guangzhou or Chongqing and food or walking interests. It presents two directions when both interests are present; a single interest starts that direction without inventing a second choice. Input is limited to 4000 UTF-16 units. The editable day labels are local and relative; they contain no inferred venue, route, opening hour, arrival time, price or feasibility claim. Unsupported requests ask for more input instead of generating a plan.

The signed-in native Ask composer offers **Plan from this message** for a supported new-goal input after the existing Ask consent gate. It opens the existing outline editor in a sheet, consumes that exact input once and generates the outline before a Trip or start date is required. Opening the sheet neither submits an Ask turn nor writes a Trip. The original composer and underlying scroll layout are retained. The keyboard is dismissed on entry; an explicit return action warns before discarding unsubmitted edits. An account-scope change dismisses the sheet. Grounded Ask reads suspend while it covers the conversation and revalidate on return. Existing events, retries and writer contracts are unchanged.

Selecting or creating a Trip inside this sheet preserves the local outline. Editing its source text invalidates the previous generated titles until regeneration, so a changed request cannot silently submit the older outline. The UI states that only city, duration and food/walking interests are consumed: other input constraints, budget and fixed plans need review. An entered date is not inferred from prose. Date binding and overlap checks do not establish general time/budget/fixed-item feasibility.

The existing Trip patch contract accepts only calendar dates. The outline is therefore not a saved Trip or executable itinerary. After the user explicitly enters a valid start date, the native client assigns consecutive `Asia/Shanghai` dates and appends new days/items to a local draft. It rejects overlap with an existing day and preserves all existing day/item IDs and fields. The current Proposal → visible diff → exact-version confirmation → native/Web reload path remains the only write path. A rejected or stale proposal does not apply the outline.

This slice does not consume saved cross-Trip preferences or a live model, maps, knowledge, route, booking or budget source. Full VPJ-09 acceptance still requires those applicable inputs, broader request handling and target-environment Chat → Plan verification. Original evidence: `artifacts/VPJ-09/verification.md`; current increment: `artifacts/VPJ-09/chat-plan-20260922/verification.md`.
