# 开发约束优化记录

维护任务：[#255](https://github.com/JTCAO515/VP-V4/issues/255)。

依据：JT要求检查并优化不符合实际开发的约束。核对基线为
`main@1bd1ffb4bc91afeffbc806035f4d56f375d50359`（2026-09-08）。
范围为开发治理、任务状态校验和启动说明；产品验收与现有安全合同保留。

| 原问题 | 证据与影响 | 本次调整 |
| --- | --- | --- |
| 所有任务永远blocked | #253/#254已合并，65个任务仍blocked；verifyNewRemote强制全部open+blocked | 校验区分基线前、开发中、completed；无未完成依赖的blocked任务提示复查，不自动认定ready |
| 改文档也要求全套检查 | AGENTS统一要求lint/typecheck/build/test及双端浏览器QA | 本地验证按改动范围；完整Issue和现有CI门保留，同版本CI证据可复用 |
| 环境未就绪就无法推进 | ADR-0023及AFK将整体依赖当全部实现前置 | 明确有现成合同支撑的独立准备范围；实际集成未验证时父任务保持open |
| 一个Issue只能一个PR | 多处强制一Issue/branch/worktree/PR | 一项连贯结果一PR，Issue可增量交付，worktree按隔离需要使用 |
| 老文件锁阻碍维护 | 活跃入口仍含56KB旧合同、AI-07a专属workflow等约束 | 活跃入口压缩为当前流程和红线；旧合同在原始归档保留，必要相邻修改记录原因并协调 |
| 小维护无执行行无法开始 | 缺VPJ执行行一律禁止开工 | 明确授权的修复/文档/工具维护用简短Issue/PR brief；不新建产品队列 |
| 反复读取和更新文档 | 多份读取顺序及每循环同步三份handoff | 首次/范围变化读全局，日常按需；handoff.json只在共享变化或交接时更新，增加render-handoff |
| 产品永远不能描述AI/持久化 | AGENTS blanket禁止声明任何真实能力 | 按版本、环境与实际证据描述；demo与计划不冒充可用产品 |
| 旧里程碑规则与VPJ冲突 | triage-labels仍以旧AI任务定义阶段硬门 | 使用当前VPJ依赖与优先级；旧规则退出执行入口 |

## 当前队列需要怎样处理

#188 VPJ-01与#202 VPJ-62的native blocker列表为空，但设备、接口、数据和其他条件仍需按选定范围检查。
本次未自动改65个任务的标签、关闭任务或删除依赖；“没有native blocker”不等于已具备全部运行条件。
合并后执行开发session应先核实候选任务，再同步其真实状态。

保留65个任务、198条依赖、每项产品验收和外部条件。manifest里的baseline-pr-pending改为planned，
并明确该字段是规划快照；运行状态以实时GitHub及环境为准。ADR-0024只调整开发流程，合并后生效。

## 验证与限制

- 11项相关测试通过：基线前后、completed、未完成/取消依赖、迁移快照保护、DAG和既有身份/安全约束。
- 校验脚本语法、生成执行合同头一致性、handoff生成和diff检查通过。
- 本地完整docs检查因读取快照缺少4个超过1MB的归档PNG而未完成；完整仓库CI需复验归档hash。
- 未执行设备、数据库、模型或生产验收；本次无产品运行代码变更。

回滚：revert本治理PR即可。没有数据库或用户数据回滚。新的规则不授予自身合并、生产、付款或外部账户权限。
