# Astra 文档与 Issue 规划优化

本次使用 `gpt-6-astra`、`xhigh` 对 README/入口文档进行改写，并独立核查任务语义和生成/同步工具。工作从 `b5acf58` 的隔离分支开始；后续PR290的Staging26/合成授权记录已一并保留，实际主线和远端读回在交付时复核。范围是文档、任务责任与相关治理工具，不改变产品功能、数据/模型权限或发布保护。

## 已定位的问题与调整

| 问题 | 调整 | 保留的边界 |
| --- | --- | --- |
| 根README与主报告混用初始库存和当前状态 | 根入口按源码/证据导航；主报告旧计数标明2026-09-05快照 | 不把本地、Staging或原生预览外推成完整产品 |
| Program/Kickoff反复要求固定PR253和生成交接 | 读取当前任务、真实前置及受影响合同；共享状态变化才更新handoff | 原业务与实际验收门仍执行 |
| 38张任务默认链接旧规划分支 | 当前执行文档默认main；历史证据继续使用明确固定版本 | 只修执行入口，不改写历史来源 |
| 全文同步可能抹掉勾选或追加进度 | 批量预检、保留已识别进度与尾部，未知漂移拒绝覆盖；普通验证核当前条款并接受勾选 | 未知漂移由agent核对来源作受控合并，不自动等同需新增人工审批 |
| #188等待后续身份/Trip才能出现的真实选区 | #188保留基础原生/iOS17/可访问性/通用呈现；#198负责真实选区Ask/确认/重载，#233/#242联合验收 | 不改212条原生依赖，不免除实际UI与设备失败 |
| 交接重复29条历史验证 | 当前摘要保留7组证据入口，完整原JSON另存历史快照 | 未决/失败不因压缩变为通过，原记录可恢复 |

#188仍有既有iOS17裁切和VoiceOver等未验项，职责澄清不关闭该票。其他三票仅明确已有真实链路的验收责任；旧条款、新条款和依赖保留在 [变更清单](astra-issue-delta-2026-09-10.json)。

## 文档权威

- `issue-plan.json` 定义当前任务、验收和依赖；生成的执行行/正文不是完成进度。
- 实时Issue/PR、实际版本与环境证据建立执行状态；当前链接和历史固定SHA分别使用。
- `docs/handoff.json` 保存当前摘要；[完整优化前交接](../archive/2026-09-10/handoff-before-astra-docs.json)保留原29条记录及字段。
- 历史交接中记录的授权不自动扩大到新对象或环境；同一范围的既有有效授权继续有效。

历史JSON的SHA256：`46785779cd18078ec1dcbfecdd60b1f380c9b6db46abbcd1a4233e5c627e0e8e`。历史快照不纳入当前调度输入。

## 验证与回滚

本次检查覆盖生成器进度保留/漂移拒绝、生命周期与依赖、docs/链接/JSON及73项任务定义；实际结果附在本次PR和远端读回记录。所有运行功能、模型/数据库操作、真实设备或产品发布验收均不由该维护变更建立。

回滚仅回退本PR，或按变更前快照恢复对应文档与Issue入口；保留这期间新进度、评论、状态和标签。不得回滚已经应用的数据库历史、用户确认或消费记录。

## 定点发布记录

已安全更新65张任务的当前执行入口及Program #187入口，并澄清#188/#198/#233/#242的验收归属；所有写入只修改正文，逐次验证状态、标签、评论与已勾选数量保留，未写原生依赖。见[发布摘要与正文hash](astra-tracker-update-2026-09-10.json)。普通读回仍严格核全部73项验收定义、212条依赖及生命周期，运行验收另行成立。

独立Astra审阅发现的迁移表标题列和HTML原始块误计问题已修复；37项治理测试、语法与文档检查通过。最终提交的独立复核和CI见本维护PR，不能将这些检查理解为产品运行通过。
