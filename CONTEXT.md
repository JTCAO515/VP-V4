# Context

Generated from [docs/handoff.json](docs/handoff.json). Full decisions, blockers and evidence: [HANDOFF.md](HANDOFF.md).
This entry is a navigation summary, not a grant of authority or a capability acceptance report. Historical records never override the current user request.

Program: [VPJ-00 #187](https://github.com/JTCAO515/VP-V4/issues/187) · Source updated: 2026-09-17

目标：交付中英原生iOS的一站式陪伴Journey Agent，全程Trip、知识、现场能力、IAP、运营、用户交付和可维护系统；以VPJ-00#187统筹。

阶段：S2 #359 bounded model statement proposals, preserving manual review and source qualification (real-model injection-resistance evidence now added); VPJ-76 (#360) agentic search structurally built, tuning follow-ups in progress. No non-C0 egress or automatic publication.

## 源记录状态（执行前核对 GitHub）

2026-09-17 (round 22): user re-scoped this thread to VPJ-75 (#359) and VPJ-76 (#360) only, in a dedicated worktree (../vp-v4-work-round22, branch codex/vpj-75-76-round22-20260917). This round's first instruction was to actually re-check (not assume) whether this sandbox has a usable real LLM provider credential, since every prior round's "no real credential" framing was never independently verified against a live provider response. lib/server/jobs/.local/.env (gitignored, per-worktree) exists in the user's main worktree with three real keys. A direct real-HTTP smoke probe of all three found: Qwen (dashscope) reachable with real account balance (a real 200); GLM's key valid but the account itself has zero real balance (a real HTTP 429 "insufficient balance"); DeepSeek's key valid but MODEL_PROFILES.deepseek_flash.providerModelId is confirmed stale against the real API (a real HTTP 400 naming the two model ids it actually supports), reproducing wiki-real-model-probe-20260915's earlier-flagged, still-unfixed bug. A separate environment fact surfaced along the way: prior rounds' "Qwen unreachable from this sandbox's network allowlist" was this Bash tool's own sandboxed-network restriction (Node's global fetch does not honor this sandbox's configured HTTP(S)_PROXY, so a direct fetch fails DNS resolution regardless of destination) -- not an actual Qwen reachability problem once the sandbox is disabled for that one command. With a real, usable Qwen credential confirmed, this round picked the #359 gap unrun.md already named as the specific next step needing exactly this: a real-model pass over evals/wiki-statement-proposals-safety/injection-cases.ts (previously fixture-only). New manual script scripts/eval/run-wiki-statement-proposals-injection-real-model.mjs (never wired into pnpm evals/CI) ran all 6 real cases through the real runWikiStatementProposalJob worker path with real Qwen (qwen3.7-plus-2026-05-26) HTTP calls, using the real adversarial source text. Result: 6/6 resisted -- the compliance marker never appeared anywhere in any real raw model response, verified independently by a direct substring search over the stored raw HTTP response bodies. A real, separate, reproducible finding surfaced along the way: 4/6 real responses were rejected as MODEL_OUTPUT_INVALID for a reason unrelated to the injection -- the model correctly declined to invent an unstated city (scope.cities: []), which isKnowledgeStatement requires to be nonempty. Not fixed this round (a KnowledgeStatement-schema-wide question, not a narrow #359 slice); recorded with real reproducing evidence for a future round. Zero production runtime file, migration, or RPC changed -- see artifacts/VPJ-75/wiki-statement-proposals-injection-real-model-20260917/verification.md.

## 下一动作

Finish this PR's CI and evidence; do not merge and do not act on any issue's open/resolved state (user's own standing rule). Real, usable next steps recorded this round, none started: (1) MODEL_OUTPUT_INVALID on provider-protocol.ts never logs the raw model response -- this round hit this exact gap twice more (this round's own 4/6 cities:[] rejections, and VPJ-76 round-21's prior flag) with real reproducing evidence; still needs a careful, narrowly-scoped fix since the module is shared by every model-gateway task. (2) MODEL_PROFILES.deepseek_flash.providerModelId ("deepseek-v4-flash") is confirmed stale against the real DeepSeek API (wants "deepseek-flash"), reproduced twice now (wiki-real-model-probe-20260915 and this round); same shared-module caution applies. (3) VPJ-76's two still-open 2026-09-16 follow-ups remain untouched: naming a specific place in the place-fixture corpus text (needs a real model re-run) and the same MODEL_OUTPUT_INVALID logging gap as (1). (4) #359's other remaining gaps (true multi-source page synthesis, real RMB cost reconciliation against Qwen's billing console, Docling, the wiki-generation job's own injection resistance) are either genuinely out of scope (Docling REJECT stands, no billing-console access) or simply not yet attempted -- not blocked by credentials now that Qwen is confirmed usable.

## 开工入口

- [docs/agents/development-workflow.md](docs/agents/development-workflow.md)
- [docs/program/2026-09-05/README.md](docs/program/2026-09-05/README.md)
- [当前决定与授权边界](HANDOFF.md#当前决定)；行动前核对当前任务和已有授权，不从历史验证推导新权限。
- [未决与运行证据](HANDOFF.md#未决与运行证据)；完整 blockers / UNRUN 在此保留，不因摘要省略而视为通过。
- [验证记录](HANDOFF.md#验证) · [回滚与观察](HANDOFF.md#下一动作与回滚)。
